# calculator-by-using-python-function-if-else-
This Python program creates a simple calculator using a loop. It asks the user for two numbers and an operation (+, -, *, /). It performs the calculation and shows the result. It handles invalid input and division by zero. The program keeps running until the user stops it using Ctrl+C.
